# config

工具箱本地配置放这里，例如：

- 板端 IP、用户名、常用端口。
- CANN 路径和自定义 OPP 默认安装路径。
- 最近使用的本地/板端目录。

当前约定：

- `default_config.json`：项目默认配置，可以提交。
- `toolbox_config.json`：用户点击“保存配置”后生成，本机偏好配置。

当前学习板使用默认密码，工具箱支持“记住密码”。如果后续换成正式设备或共享仓库，请不要提交真实密码；可以取消“记住密码”，让工具箱只在运行时读取密码输入框。
